#include<iostream>
using namespace std;

int main(){
    int num1, num2;
    
    cout<<"Enter the value of num1\n"; //<< is called as incercion operator
    cin>>num1;//>>is called as extraction operator

    cout<<"Enter the value of num2\n"; //<< is called as incercion operator
    cin>>num2;//>>is called as extraction operator

    cout<<"the sum is "<<num1+num2;
    return 0;
}